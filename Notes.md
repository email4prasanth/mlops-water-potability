- Create terraform code IOC with ec2 instance
- Upload the model.pkl file in the **water-potability** folder with dockerfile to deploy
- Using workflow deploy the container by pushing the image from ECR 
```sh
http://<Public-IP>:8000
http://<Public-IP>:8000/docs
```